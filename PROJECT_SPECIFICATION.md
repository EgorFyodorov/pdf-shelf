# PDF Shelf - Телеграм-бот для управления персональной библиотекой PDF материалов

## Описание проекта

PDF Shelf — интеллектуальный телеграм-бот для создания и управления персональной библиотекой PDF материалов. Бот позволяет пользователям сохранять веб-статьи и документы в формате PDF, автоматически анализировать их содержимое с помощью LLM (объём, сложность, тематика) и умно подбирать материалы для чтения на основе доступного времени и интересов пользователя.

---

## Пользовательские сценарии

## Загрузка материалов

### Загрузка ссылок в библиотеку

**Пользователь:** отправляет одну или несколько ссылок в одном сообщении

**Бот отвечает:**
1. Распознаёт все URL в сообщении
2. Для каждой ссылки:
   - Анализирует страницу через MCP
   - Конвертирует страницу в PDF
   - Отправляет PDF пользователю с карточкой анализа:
     ```
     📄 "Как устроен X"
     example.com
     
     Объём: 12 стр. (1.3 МБ) • 2.8k слов (11 мин)
     Сложность: Средняя
     Темы: ML, Наука
     ```
   - Получает `file_id` из ответа Telegram API
   - Сохраняет ссылку в библиотеку с метаданными анализа и `telegram_file_id`

3. **Если загружена одна ссылка:**
   - Подтверждает: "✓ Сохранено в библиотеку"

4. **Если загружено несколько ссылок:**
   - Отправляет итоговое сообщение:
     ```
     ✓ Добавлено 3 материала:
     
     1. example.com/article1 (15 мин) — Технологии
     2. example.com/article2 (8 мин) — Бизнес
     3. example.com/article3 (22 мин) — Наука
     
     Всего: 45 минут чтения
     ```

**Результат:** Все ссылки добавлены в библиотеку, PDF сконвертированы и отправлены (получены `file_id`). При выгрузке будут переиспользованы сохранённые PDF по `file_id`.

---

## Выгрузка материалов

### Выгрузка PDF

**Параметры запроса:**
- **Время** — обязательный параметр. Без указания времени бот запросит уточнение через кнопки-саджесты в Telegram.
- **Теги** — опциональный параметр. Можно указать теги для фильтрации материалов по тематике.

**Примеры запросов:**
- "у меня есть 30 минут" — выгрузка без фильтрации по тегам
- "выгрузи на час чтения" — выгрузка без фильтрации по тегам
- "выгрузи материалы по ML, у меня есть час" — выгрузка с фильтрацией по тегам
- "статьи про экономику на 30 минут" — выгрузка с фильтрацией по тегам
- "выгрузи материалы" — без времени, бот запросит уточнение
- "выгрузи материалы по ML" — без времени, бот запросит уточнение

**Сценарий работы бота:**

1. **Если время указано:**
   - Анализирует библиотеку пользователя
   - Извлекает доступное время из запроса (LLM парсит: "30 минут", "час", "20 мин", "полтора часа" и т.д.)
   - Если указаны теги — фильтрует материалы по тегам из MCP-анализа
   - Подбирает материалы, которые умещаются в указанное время, используя алгоритм
   - Сразу отправляет все PDF отдельными сообщениями с карточками анализа:
     ```
     📄 "Как устроен X"
     example.com
     
     Объём: 12 стр. (1.3 МБ) • 2.8k слов (11 мин)
     Сложность: Средняя • ML, Наука
     ```
   - После отправки всех файлов отправляет итоговое сообщение:
     ```
     ✅ Отправлено 3 материала (30 минут чтения)
     ```

2. **Если время не указано:**
   - Бот отправляет сообщение с запросом времени и показывает кнопки-саджесты в Telegram:
     ```
     Сколько времени у вас есть для чтения?
     ```
     Кнопки-саджесты: "15 минут", "30 минут", "1 час", "2 часа"
   - После выбора времени или указания времени в тексте бот работает по сценарию выше

**Если материалов недостаточно:**
- Отправляет все доступные PDF отдельными сообщениями
- Сообщает о недостатке материалов:
  ```
  ✅ Отправлено 2 материала (15 минут чтения)
  
  В библиотеке больше нет материалов на указанное время.
  ```

---

## Структура базы данных

### Таблицы

**`users`** — пользователи бота
- `user_id` (INTEGER, PRIMARY KEY) — Telegram user_id
- `user_name` (TEXT) — Имя пользователя
- `files_count` (INTEGER) — Счётчик файлов пользователя

**`files`** — сохранённые PDF материалы
- `file_id` (UUID, PRIMARY KEY) — Внутренний UUID файла
- `telegram_file_id` (TEXT) — Telegram file_id для переиспользования PDF
- `user_id` (INTEGER, FK → users) — Владелец файла
- `source_url` (TEXT) — Исходный URL (если материал был загружен по ссылке)
- `title` (TEXT) — Название документа
- `reading_time_min` (NUMERIC) — Время чтения в минутах (критично для алгоритма подбора и индексации)
- `tags` (TEXT[]) — Массив тегов для быстрой фильтрации (извлекается из `analysis_json.topics[].label`)
- `analysis_json` (JSONB) — Полный JSON анализа (все данные из MCP анализа хранятся здесь)
- `created_at` (TIMESTAMP) — Дата создания записи

**Примечание:** 
- Все данные анализа (complexity, topics, category, volume и т.д.) хранятся в `analysis_json`
- Отдельные поля `reading_time_min` и `tags` оставлены для производительности: быстрая фильтрация по тегам и сортировка по времени чтения
- `tags` содержит только метки (labels) из `topics`, полная информация о темах (score, keywords, rationale) остаётся в `analysis_json`

**`requests`** — история выданных материалов
- `id` (UUID, PRIMARY KEY) — UUID запроса
- `user_id` (INTEGER, FK → users) — Пользователь, которому выдан материал
- `file_id` (UUID, FK → files) — Выданный файл

### Индексы

- `idx_files_user_id` — поиск файлов пользователя
- `idx_files_reading_time` — сортировка по времени чтения
- `idx_files_user_reading_time` — комбинированный индекс для подбора материалов
- `idx_files_tags` (GIN) — поиск по тегам (массив TEXT[])
- `idx_files_analysis_json` (GIN) — поиск по полям внутри JSON (category, complexity)
- `idx_requests_user_id` — история запросов пользователя
- `idx_requests_file_id` — статистика по файлам

**Примечание:** 
- Для фильтрации по тегам используется индекс на массиве `tags` (быстрее и проще, чем JSONB-запросы)
- Для других полей анализа (category, complexity) используются JSONB-запросы с GIN-индексом на `analysis_json`

---

## Данные из JSON анализа для хранения

При анализе PDF через MCP (`mcp_pdf/schema.py::ANALYSIS_JSON_SCHEMA`) извлекаются следующие данные:

### Обязательные для хранения в БД:

1. **`volume.reading_time_min`** — время чтения в минутах (критично для алгоритма подбора материалов)
2. **`volume.word_count`** — количество слов (для отображения в карточке)
3. **`volume.page_count`** — количество страниц (для отображения)
4. **`volume.byte_size`** — размер файла в байтах (для отображения)
5. **`complexity.score`** — оценка сложности 1-100 (для фильтрации/сортировки)
6. **`complexity.level`** — уровень сложности (для отображения: "Низкая", "Средняя", "Высокая")
7. **`topics[]`** — массив тем (для фильтрации по тегам в запросах пользователя)
8. **`category.label`** — основная категория документа
9. **`category`** — полный объект категории
10. **Полный JSON анализа** — сохраняется в `analysis_json` для возможности восстановления и расширения функционала

### Маппинг данных в БД:

```
Полный JSON анализа → files.analysis_json (JSONB)
analysis.volume.reading_time_min → files.reading_time_min (отдельное поле для индексации)
analysis.topics[].label → files.tags (TEXT[] для быстрой фильтрации)
```

**Все остальные данные** (volume, complexity, полная информация о topics, category и т.д.) хранятся в `analysis_json` и извлекаются через JSONB-запросы:

```sql
-- Примеры запросов к JSONB:
analysis_json->'volume'->>'word_count'  -- количество слов
analysis_json->'volume'->>'page_count'  -- количество страниц
analysis_json->'volume'->>'byte_size'  -- размер файла
analysis_json->'complexity'->>'level'  -- уровень сложности
analysis_json->'complexity'->>'score'  -- оценка сложности
analysis_json->'topics'                 -- массив тем (для фильтрации)
analysis_json->'category'->>'label'    -- категория
```

### Использование данных в пользовательском интерфейсе:

**Карточка анализа (при загрузке и выгрузке):**
- `title` — из `files.title` или `analysis_json.category.label`
- `source_url` — из `files.source_url`
- `page_count`, `byte_size`, `word_count` — из `analysis_json->'volume'`
- `reading_time_min` — из `files.reading_time_min` (отдельное поле)
- `complexity_level` — из `analysis_json->'complexity'->>'level'`
- `topics` — первые 2-3 темы из `analysis_json->'topics'`

**Фильтрация при выгрузке:**
- По тегам: запрос к массиву `tags` с использованием GIN-индекса
  ```sql
  WHERE tags && ARRAY['ML']::TEXT[]  -- файлы, содержащие тег "ML"
  ```
- По времени: использование `files.reading_time_min` для алгоритма подбора (прямое поле для производительности)
- Исключение уже выданных: проверка через таблицу `requests`

---

## Техническая архитектура

### Составные части проекта

Проект состоит из следующих основных компонентов:

1. **Telegram Bot** (`project/bot.py`, `project/handlers/main_handlers.py`)
   - Основан на aiogram 3.x
   - Обрабатывает команды и сообщения пользователей
   - Управляет жизненным циклом бота и диспетчеризацией событий

2. **MCP PDF Analysis** (`project/mcp_pdf/`)
   - **schema.py**: Определяет JSON-схемы для анализа PDF и промпты для LLM
   - **tools.py**: Инструменты для анализа PDF через Gemini API
   - **pdf_utils.py**: Утилиты для извлечения текста, подсчёта слов, определения языка
   - **metrics.py**: Точный расчёт времени чтения с учётом сложности, изображений, таблиц, кода
   - **server.py**: MCP-сервер для интеграции с внешними системами

3. **Parser** (`project/parser/parser.py`)
   - Конвертирует веб-страницы в PDF через Playwright
   - Скачивает прямые PDF-ссылки
   - Обрабатывает HTML-страницы и сохраняет их как PDF

4. **Database Layer** (`project/database/`)
   - **models.py**: SQLAlchemy ORM модели (User, File, Request)
   - **engine.py**: Инициализация подключения к PostgreSQL
   - **user_repository.py**: CRUD операции для пользователей
   - **file_repository.py**: CRUD операции для файлов
   - **pdf_repository.py**: Заглушка для логирования
   - **request_repository.py**: CRUD операции для запросов на выгрузку

5. **API Layer** (`project/api/pdf_analysis.py`)
   - Публичный API для анализа PDF
   - Обёртки над MCP-инструментами с обработкой ошибок и таймаутов

6. **Configuration** (`project/config.py`, `project/schemas/config_schemas.py`)
   - Загрузка конфигурации из YAML
   - Переопределение через переменные окружения

### Взаимодействие компонентов

```
┌─────────────────┐
│  Telegram User  │
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────────┐
│      Telegram Bot (aiogram)         │
│  ┌───────────────────────────────┐  │
│  │   Handlers (main_handlers)    │  │
│  └───────────┬───────────────────┘  │
└──────────────┼──────────────────────┘
               │
    ┌──────────┼──────────┐
    │          │          │
    ▼          ▼          ▼
┌─────────┐ ┌─────────┐ ┌──────────────┐
│ Parser  │ │   API   │ │  Database    │
│(Playwr.)│ │(Analysis)│ │ (PostgreSQL) │
└────┬────┘ └────┬────┘ └──────┬───────┘
     │           │              │
     │           ▼              │
     │    ┌──────────────┐      │
     │    │  MCP PDF     │      │
     │    │  (Gemini)    │      │
     │    └──────────────┘      │
     │                          │
     └──────────────────────────┘
```

### Потоки обработки

**Поток обработки загрузки ссылки:**

1. Пользователь отправляет URL → `main_handlers.py`
2. Хэндлер вызывает `Parser.parse()` для конвертации URL → PDF
3. PDF сохраняется временно, затем отправляется в Telegram через `bot.send_document()`
4. Получаем `telegram_file_id` из ответа Telegram API
5. Вызываем `api/pdf_analysis.py::analyze_pdf_path()` для анализа
6. Анализ использует `mcp_pdf/tools.py::extract_pdf_tool()` и `analyze_text_tool()`
7. Результат анализа сохраняется в БД через `FileRepository.create_file()`
8. Сохраняем метаданные анализа в JSON-поле `analysis_json`

**Поток обработки запроса на выгрузку:**

1. Пользователь отправляет запрос "у меня есть 30 минут" → `main_handlers.py`
2. LLM парсит запрос и извлекает время и опциональные теги
3. Запрос к БД через `FileRepository.get_files_by_user()` с фильтрацией по тегам
4. Алгоритм подбора материалов (knapsack или greedy) по времени чтения
5. Для каждого выбранного файла:
   - Получаем `telegram_file_id` из БД
   - Отправляем PDF через `bot.send_document(file_id=telegram_file_id)`
   - Создаём запись в `Request` через `RequestRepository.create_request()`

### Контракты (API и схемы данных)

#### JSON-схема анализа PDF (`mcp_pdf/schema.py::ANALYSIS_JSON_SCHEMA`)

```json
{
  "doc_language": "ru" | "en" | ...,
  "volume": {
    "word_count": integer,
    "char_count": integer,
    "page_count": integer | null,
    "byte_size": integer | null,
    "reading_time_min": number,
    "method": {
      "word_count": "precomputed" | "content_based_full_scan",
      "char_count": "estimated_no_spaces"
    }
  },
  "complexity": {
    "score": integer,              // 1-100
    "level": "очень низкая" | "низкая" | "средняя" | "высокая" | "очень высокая",
    "estimated_grade": string,     // "школьный", "вузовский", и т.д.
    "drivers": string[],           // Ключевые факторы сложности
    "notes": string
  },
  "topics": [
    {
      "label": string,             // "ML", "Наука", "Технологии"
      "score": number,              // 0.0-1.0
      "keywords": string[],
      "rationale": string
    }
  ],                                // Максимум 6 элементов
  "category": {
    "label": string,                // "Другое", "Научная статья", и т.д.
    "score": number,
    "basis": string,                // "first_page" | "filename" | "toc" | "multi" | "unknown"
    "keywords": string[]
  },
  "limitations": {
    "short_or_noisy_input": boolean,
    "comments": string
  }
}
```

#### API контракты (`project/api/pdf_analysis.py`)

- `extract_pdf(path: str | None, url: str | None) -> Tuple[str, dict]`
  - Возвращает `(TEXT, META)` где TEXT — первая страница, META — метаданные
  
- `analyze_text(text: str, meta: dict | None) -> dict`
  - Возвращает JSON по `ANALYSIS_JSON_SCHEMA`
  
- `analyze_pdf_path(path: str) -> dict`
  - Удобная функция: извлечь + проанализировать
  
- `analyze_pdf_url(url: str) -> dict`
  - Удобная функция: скачать + извлечь + проанализировать

#### Контракт Telegram Bot

**Входные сообщения:**
- Текст с URL(ами) → загрузка материалов
- Текст с запросом времени ("у меня есть 30 минут") → выгрузка материалов
- Команда `/start` → приветствие

**Выходные сообщения:**
- PDF файлы с карточками анализа
- Текстовые подтверждения и итоговые сообщения

---

## Техническая реализация выгрузки материалов

### Парсинг запроса пользователя

**Вход:** Текстовое сообщение пользователя
- "у меня есть 30 минут"
- "выгрузи на час чтения"
- "выгрузи материалы по ML, у меня есть час"
- "статьи про экономику на 30 минут"

**Обработка:**
- Использовать LLM (Gemini) для извлечения структурированных данных:
  ```python
  {
    "time_minutes": 30,           # Извлечённое время в минутах
    "tags": ["ML"],               # Опциональные теги для фильтрации
    "has_time": true              # Флаг наличия времени
  }
  ```

**Если время не указано:**
- Отправить сообщение с запросом времени
- Показать кнопки-саджесты: "15 минут", "30 минут", "1 час", "2 часа"

### Запрос к базе данных

**SQL-запрос с фильтрацией:**

```python
# Псевдокод
files = await file_repo.get_files_by_user_filtered(
    user_id=user_id,
    tags=["ML"] if tags else None,  # Фильтр по topics[].label
    exclude_requested=True           # Исключить уже выданные файлы
)

# Сортировка по приоритету (опционально):
# - По времени чтения (от меньшего к большему)
# - По сложности (от меньшей к большей)
# - По дате добавления (новые первыми)
```

**Требуемые поля из БД:**
- `file_id` (UUID)
- `telegram_file_id` (TEXT) — для отправки PDF
- `reading_time_min` (NUMERIC) — для алгоритма подбора
- `tags` (TEXT[]) — для фильтрации по тегам
- `title` (TEXT) — для карточки
- `source_url` (TEXT) — для карточки
- `analysis_json` (JSONB) — все данные анализа (complexity, topics, category, volume)

### Алгоритм подбора материалов

**Задача:** Подобрать набор файлов, суммарное время чтения которых ≤ доступного времени, максимизируя использование времени.

**Варианты алгоритмов:**

**A. Greedy (жадный алгоритм) — простой и быстрый:**
```python
def select_files_greedy(files: List[File], available_time: float) -> List[File]:
    selected = []
    remaining_time = available_time
    
    # Сортируем по времени чтения (от меньшего к большему)
    sorted_files = sorted(files, key=lambda f: f.reading_time_min)
    
    for file in sorted_files:
        if file.reading_time_min <= remaining_time:
            selected.append(file)
            remaining_time -= file.reading_time_min
    
    return selected
```

**B. Knapsack (рюкзак) — оптимальный, но медленнее:**
```python
def select_files_knapsack(files: List[File], available_time: float) -> List[File]:
    # Динамическое программирование для задачи о рюкзаке
    # Максимизируем количество файлов или суммарное время чтения
    # ...
```

**Рекомендация:** Начать с Greedy, при необходимости перейти на Knapsack.

### Отправка материалов пользователю

**Для каждого выбранного файла:**

1. **Отправка PDF:**
   ```python
   await bot.send_document(
       chat_id=user_id,
       document=file.telegram_file_id,  # Используем сохранённый file_id
       caption=format_file_card(file)    # Карточка анализа
   )
   ```

2. **Форматирование карточки:**
   ```python
   def format_file_card(file: File) -> str:
       analysis = file.analysis_json
       title = file.title or file.source_url or analysis.get("category", {}).get("label", "Документ")
       volume = analysis.get("volume", {})
       page_count = volume.get("page_count", "?")
       byte_size_mb = (volume.get("byte_size", 0) / 1024 / 1024)
       word_count_k = volume.get("word_count", 0) / 1000
       reading_time = file.reading_time_min
       complexity = analysis.get("complexity", {}).get("level", "Средняя")
       topics = ", ".join([t["label"] for t in analysis.get("topics", [])[:3]])
       
       return f"""📄 "{title}"
{file.source_url}

Объём: {page_count} стр. ({byte_size_mb:.1f} МБ) • {word_count_k:.1f}k слов ({reading_time:.0f} мин)
Сложность: {complexity} • {topics}"""
   ```

3. **Создание записи в `requests`:**
   ```python
   await request_repo.create_request(
       user_id=user_id,
       file_id=file.file_id
   )
   ```

4. **Итоговое сообщение:**
   ```python
   total_time = sum(f.reading_time_min for f in selected_files)
   await bot.send_message(
       chat_id=user_id,
       text=f"✅ Отправлено {len(selected_files)} материала (всего {total_time:.0f} минут чтения)"
   )
   ```

### Обработка недостатка материалов

**Если суммарное время всех доступных файлов < запрошенного времени:**

- Отправить все доступные файлы
- Добавить предупреждение:
  ```
  ✅ Отправлено 2 материала (15 минут чтения)
  
  В библиотеке больше нет материалов на указанное время.
  ```

---
